/**
 * @sentry/react の型宣言（ローカル環境フォールバック）
 *
 * @sentry/react パッケージが正常にインストールされている場合（CI など）は
 * パッケージ自体の型定義が使用され、この宣言は無視される。
 * ローカル環境でパッケージが壊れている場合のフォールバックとして機能する。
 */
declare module '@sentry/react';
